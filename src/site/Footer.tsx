import type { ReactNode } from 'react';
import './Footer.css';

export function Footer(): ReactNode {
  return (
    <footer className="site-footer">
      <p>Données publiques françaises, notamment INSEE.</p>
    </footer>
  );
}
